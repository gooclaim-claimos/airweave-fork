apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "datasources-backend.name" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "datasources-backend.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
